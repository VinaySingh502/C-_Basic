# C# Learning Console App

This beginner project demonstrates:

- Variables and common data types
- Arithmetic and methods
- `if`/`else` conditions
- Lists and `foreach` loops
- Classes, properties, constructors, and objects

Run it from this directory:

```powershell
dotnet run
```

Try changing the learner details, numbers, and topic list in `Program.cs`.
